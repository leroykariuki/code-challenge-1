mark = int(input("Enter the student's mark (between 0 and 100): "))

if mark > 100 or mark < 0:
    print("Invalid mark! Please enter a mark between 0 and 100.")
elif mark > 79:
    grade = "A"
elif mark >= 60:
    grade = "B"
elif mark >= 50:
    grade = "C"
elif mark >= 40:
    grade = "D"
else:
    grade = "E"

print("Grade: " + grade)